package router

import (
	"github.com/gofiber/fiber/v2"
)

func PlayerRouter(app fiber.Router) {
	//app.Post("/create", handler.RequestReaction(service))
	//app.Put("/update", handler.RegisterPlayerAction(service))
	//app.Post("/login", handler.Login())
}
